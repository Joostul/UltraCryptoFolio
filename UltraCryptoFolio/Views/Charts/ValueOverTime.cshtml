
@{
    ViewData["Title"] = "ValueOverTime";
}

@{await Html.RenderPartialAsync("_GraphsMenuPartial");}

<h2>Your portfolio over time (weekly)</h2>

<div class="alert alert-danger">
    <strong>Warning!</strong> Weekly portfolio performance for larger portfolios will take some time to load and will have some errors. We are working on fixing this issue.
</div>

<div id="areachartContainer"></div>

<script type="text/javascript">
    window.onload = function () {
        var chart = new CanvasJS.Chart("areachartContainer", {
            theme: "theme2",
            animationEnabled: true,
            animationEnabled: true,
            axisX: {
                title: "Week",
            },
            axisY: {
                title: "Value",
            },
            data: [
                {
                    type: "area",
                    xValueType: "dateTime",
                    dataPoints: @Html.Raw(ViewBag.Data)
                },
                {
                    type: "area",
                    xValueType: "dateTime",
                    dataPoints: @Html.Raw(ViewBag.SecondData)
                }
            ]
        });
        chart.render();
    };
</script>